{
  "name": "jenkins-cicd-demo",
  "version": "1.0.0",
  "scripts": {
    "start": "node index.js",
    "test": "npx jest --ci"
  },
  "jest": {
    "reporters": [
      "default",
      [
        "jest-junit",
        {
          "outputDirectory": "test-results",
          "outputName": "results.xml"
        }
      ]
    ]
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.2.2"
  }
}
